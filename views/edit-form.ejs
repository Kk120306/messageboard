<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <title>Edit Message</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 min-h-screen flex items-center justify-center p-6">

    <div class="w-full max-w-xl bg-white shadow-md rounded-xl p-8">
        <h1 class="text-2xl font-bold text-center text-indigo-600 mb-6">Edit Message</h1>

        <form action="/edit/<%= message.id %>" method="POST" class="space-y-6">


            <div>
                <label for="title" class="block font-medium mb-1">Title:</label>
                <input type="text" id="title" name="title" value="<%= message.title %>" required
                    class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring focus:ring-indigo-400" />
                <% const titleError=errors?.find(e=> e.path === 'title'); %>
                    <% if (titleError) { %>
                        <p class="text-red-500 text-sm mt-1">
                            <%= titleError.msg %>
                        </p>
                        <% } %>
            </div>


            <div>
                <label for="message" class="block font-medium mb-1">Message:</label>
                <textarea id="message" name="message" rows="5" required
                    class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring focus:ring-indigo-400"><%= message.body %></textarea>
                <% const bodyError=errors?.find(e=> e.path === 'message'); %>
                    <% if (bodyError) { %>
                        <p class="text-red-500 text-sm mt-1">
                            <%= bodyError.msg %>
                        </p>
                        <% } %>
            </div>

            <div class="text-center flex flex-col items-center">
                <button type="submit"
                    class="w-full bg-indigo-600 text-white px-4 py-2 rounded-md hover:bg-indigo-700 transition">
                    Update
                </button>
                <a href="/" class="mt-4 inline-block text-blue-600 hover:text-blue-800 text-sm">Back</a>
            </div>

        </form>
    </div>

</body>

</html>