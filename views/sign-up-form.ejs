<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Sign Up</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 min-h-screen flex items-center justify-center p-6">

    <div class="w-full max-w-md bg-white rounded-xl shadow-lg p-8">
        <h1 class="text-2xl font-bold text-center text-blue-600 mb-6">Sign Up</h1>

        <form action="/sign-up" method="POST" class="space-y-4">

            <div>
                <label for="first_name" class="block font-medium">First Name</label>
                <input id="first_name" name="first_name" placeholder="First name" type="text"
                    class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring focus:ring-blue-300" />
                <% const fnameError=errors?.find(e=> e.path === 'first_name'); %>
                    <% if (fnameError) { %>
                        <p class="text-red-600 text-sm mt-1">
                            <%= fnameError.msg %>
                        </p>
                        <% } %>
            </div>


            <div>
                <label for="last_name" class="block font-medium">Last Name</label>
                <input id="last_name" name="last_name" placeholder="Last name" type="text"
                    class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring focus:ring-blue-300" />
                <% const lnameError=errors?.find(e=> e.path === 'last_name'); %>
                    <% if (lnameError) { %>
                        <p class="text-red-600 text-sm mt-1">
                            <%= lnameError.msg %>
                        </p>
                        <% } %>
            </div>


            <div>
                <label for="email" class="block font-medium">Email</label>
                <input id="email" name="email" placeholder="Email" type="email"
                    class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring focus:ring-blue-300" />
                <% const emailError=errors?.find(e=> e.path === 'email'); %>
                    <% if (emailError) { %>
                        <p class="text-red-600 text-sm mt-1">
                            <%= emailError.msg %>
                        </p>
                        <% } %>
            </div>


            <div>
                <label for="password" class="block font-medium">Password</label>
                <input id="password" name="password" type="password"
                    class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring focus:ring-blue-300" />
                <% const pwError=errors?.find(e=> e.path === 'password'); %>
                    <% if (pwError) { %>
                        <p class="text-red-600 text-sm mt-1">
                            <%= pwError.msg %>
                        </p>
                        <% } %>
            </div>

            <div>
                <label for="passwordC" class="block font-medium">Confirm Password</label>
                <input id="passwordC" name="passwordC" type="password"
                    class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring focus:ring-blue-300" />
                <% const pwCError=errors?.find(e=> e.path === 'passwordC'); %>
                    <% if (pwCError) { %>
                        <p class="text-red-600 text-sm mt-1">
                            <%= pwCError.msg %>
                        </p>
                        <% } %>
            </div>

            <div class="text-center flex flex-col items-center">
                <button type="submit" class="bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700 transition">
                    Sign Up
                </button>
                <a href="/log-in" class="text-blue-600 hover:text-blue-700 mt-4 inline-block">Already have an account?
                    Log In</a>

            </div>

        </form>
    </div>

</body>

</html>